import React, { memo } from 'react';

/**
 * ActionButtons is disabled so listing cards remain clean and uncluttered.
 * All actions (Call, WhatsApp, Instagram, Share) are housed inside the listing details modal.
 */
function ActionButtons() {
  return null;
}

export default memo(ActionButtons);